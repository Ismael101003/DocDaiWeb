import type { OCRResponse } from '@/types/documents';
export function OCRResult({ result }: { result: OCRResponse }) { return <section className="card docdai-surface border-0 rounded-4"><div className="card-body p-4"><span className="docdai-eyebrow">OCR completado</span><div className="row mt-2 g-3"><div className="col-4"><strong>{(result.confidence * 100).toFixed(2)}%</strong><small>Confianza</small></div><div className="col-4"><strong>{result.pages}</strong><small>Páginas</small></div><div className="col-4"><strong>{result.processing_time.toFixed(2)} s</strong><small>Tiempo</small></div></div><h4 className="h6 mt-4">Texto extraído</h4><pre className="docdai-ocr-text">{result.text}</pre></div></section>; }
