import type { ProcessingLogEvent } from '@/types/documents';
const icon = { processing: '⏳', success: '✓', error: '!', review: '👤' } as const;
export function ProcessingLog({ events }: { events: ProcessingLogEvent[] }) { return <section className="docdai-processing-log" aria-live="polite"><div className="d-flex justify-content-between align-items-center"><h3 className="h6 mb-0">Actividad del procesamiento</h3><span className="small text-secondary">Eventos locales confirmados</span></div>{events.length ? <div className="mt-3 d-grid gap-2">{events.map((event) => <div className={`docdai-log-row is-${event.kind}`} key={event.id}><time>{new Intl.DateTimeFormat('es-MX', { hour: '2-digit', minute: '2-digit', second: '2-digit' }).format(new Date(event.timestamp))}</time><span aria-hidden="true">{icon[event.kind]}</span><span>{event.message}</span></div>)}</div> : <p className="text-secondary small mb-0 mt-3">Los eventos aparecerán al iniciar y recibir respuesta de cada operación.</p>}</section>; }
