export function ReviewActions({ pending, onSave, onApprove }: { pending: number; onSave: () => void; onApprove: () => void }) { return <footer className="docdai-review-actions"><span><strong>Campos pendientes: {pending}</strong><small>Los cambios se conservan únicamente durante esta sesión.</small></span><div className="d-flex gap-2"><button type="button" className="btn btn-outline-primary" onClick={onSave}>Guardar cambios</button><button type="button" className="btn btn-primary" disabled={pending > 0} onClick={onApprove}>Aprobar revisión</button></div></footer>; }
