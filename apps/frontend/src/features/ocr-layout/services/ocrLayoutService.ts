// TODO BACKEND: persist field-level corrections and final clinical approval when an endpoint exists.
// The pipeline endpoints remain in src/services/documents.ts and are intentionally reused.
export const reviewPersistenceAvailable = false;
